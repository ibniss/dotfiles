.aider*
.env
.env.local
.DS_Store
# variables is generated and varies a lot
fish/.config/fish/fish_variables
# karabiner auto-generates dated backups
karabiner/.config/karabiner/automatic_backups/
# keep local git identities out of the public dotfiles repo
.gitconfig.local
.gitconfig.work
git/.gitconfig.local
git/.gitconfig.work
